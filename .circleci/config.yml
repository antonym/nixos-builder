version: 2
jobs:
  build:
    docker:
      - image: nixos/nix:latest
        environment:
          - ENV: CI
    working_directory: /

    steps:
     - run: |
         wget https://raw.githubusercontent.com/NixOS/nixpkgs/master/nixos/release.nix
         nix-build -A netboot nixos/release.nix

